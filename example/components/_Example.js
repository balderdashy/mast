/**
 * This is what an empty component looks like
 * It would control the template with id=`Example`
 * wherever that template is rendered in the DOM.
 */

FRAMEWORK.define('Example', function () {
	return {

	};
});

